# docker compose up -d 로 DB 실행 후 로컬에서 pnpm dev 할 때 사용
# .env.example 을 복사해 .env 로 만들고 값을 채우세요.

# 개발 DB (docker-compose db 서비스 기준)
DATABASE_URL=postgresql://postgres:postgres@localhost:35432/where_train
